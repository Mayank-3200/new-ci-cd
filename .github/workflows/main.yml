name: Run Playwright API Call

on:
  workflow_dispatch:   # Trigger manually

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call Playwright Run API
        run: |
          curl --location 'https://d3f4c03b6b08.ngrok-free.app/api/v1/playwright/run-tests-ciCD?id=257&email=superadmin%40yopmail.com'
